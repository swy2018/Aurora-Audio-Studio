# Aurora 文案、应用回退与设置对齐（未发布）

日期：2026-09-13。基线：cfabf93；保留前一轮第一批修复，未改版本号、未提交或推送、未生成正式安装包。

## 已修改

- Windows / Mac 自有文案改为面向用户的操作说明：统一安装、成品、模型支持与错误提示；修正 Mac 任务空状态等旧原型文案。共享与 Mac 独立词条均保留简体中文、繁体中文、英文、日文。
- 官网双语、README 双语和两端使用说明同步调整。上游工作台原文、原有工作台翻译文件、许可证及已发布历史更新日志未重写。
- 更新通道控件由 MaxWidth=420 + Stretch 改为 Width=320 + Left，与语言、主题控件一致。新增三项控件对齐/宽度的回归断言；不改变更新通道保存行为。
- Windows“关于”、Mac“设置”和“关于”新增“回退到上一个正式版”。
- 无论当前是正式版还是 Beta，回退只查找低于当前版本、不是草稿或预发布、采用正式 X.Y.Z 标签并具有当前平台安装包的最近版本。普通检查更新仍只向更高版本更新，切换通道不自动降级。
- 回退需确认当前/目标版本及旧版数据兼容风险，不自动改更新通道。下载与校验继续使用现有流程，缺少校验文件不会安装。Windows 在开始安装前复查任务，避免下载期间启动的新任务被打断；Mac 保留原有多次任务状态检查。
- Mac 应用替换不再使用 .Aurora-backups 保存长期旧应用。原应用仅暂存在本次 .Aurora-updates/<随机标识>/previous.app 中，替换并验证成功后删除；失败时尝试恢复。临时清理失败时明确报告路径，不假装已经清除。历史备份不删除，模型自身的备份/回退机制不变。
- Mac 显式回退参数必须对应所选正式版构建号且低于当前构建；仍强制 Developer ID 身份、签名与 Gatekeeper/公证检查，不采用“允许任意降级”。

## 验证

- 155 项行为及更新检查通过（前一轮 110 项 + 本轮回退/Mac 更新 45 项）。
- 独立更新流程回归通过，含设置控件左对齐断言。
- Windows 独立 Debug 验证构建：0 警告、0 错误；Core 编译通过。
- 126 个原生静态本地化键无英文缺项；原有工作台 576 条四语言条目齐全；27 项模型目录同步检查通过；官网 JavaScript 语法检查通过。
- Windows 实际点击回退按钮：2.0.0-beta.2 选择了 1.9.9 正式版，确认框显示版本与数据兼容提醒。已取消，未下载或安装旧版。
- 官网实际浏览器中英文布局检查，英文 390/1440 像素视口的页面宽度分别为 390/1440，没有横向溢出。预览服务器最初的 .mjs MIME 配置/缓存问题已通过修正本机预览服务并重开隔离浏览器排除，未更改生产网站逻辑。
- 用户正在查看旧验证窗口。新独立构建被正常单实例保护转发，未强制退出旧窗口。因此最终左对齐修正及最终构建的可见窗口尚待重开核查，不能把源码断言称为真实像素验收。英文原生 UI 自动化也未计为成功；四语言词条检查不等于逐语言视觉验收。

## 未验证 / 未执行

- 当前 Windows 无 Mac Swift 工具链和完整 Avalonia/macOS 构建依赖，Mac 完整构建、安装助手编译、签名与公证验证后的真实替换/回退及成功清理仍需 Mac 验收。已更新 Swift 的成功替换/失败恢复测试断言，尚未在 Mac 运行。
- 两个平台都没有实际降级安装，也没有执行历史备份清理。Mac 下载/版本筛选回归运行于 Windows 的 .NET 测试工程，不能替代 macOS 安装助手实测。
- 未发布或同步远端官网 / GitHub About；当前已安装版本不会因本地源码修改自动获得这些变化。
- 未改模型安装备份机制、未下载或重装模型、未运行模型短样本。

## 验证入口与证据

独立本机验证程序：../audit/copy-rollback-2026-09-13/app/Aurora Audio Studio.exe。请先退出已有 Aurora，再运行；不要绕过单实例保护。
验证配置为同目录 ../ui-state/，不覆盖正式应用配置。

- audit/copy-rollback-2026-09-13/regression.log
- audit/copy-rollback-2026-09-13/update-flow.log
- audit/first-batch-2026-09-13/rollback-confirmation.png（真实确认框）
- output/playwright/copy-desktop-zh.png、copy-mobile-zh.png、copy-mobile-en.png、copy-desktop-en.png（本机官网预览，不是新产品宣传截图）
- AppRollbackRegression.cs 复用原有 Mac AppUpdaterTests，无需 Avalonia 即可检查版本选择和下载完整性。

复测：运行 BehaviorTests，再将 work/audio-studio/AuroraAudioStudio.iss 传给 UpdateFlowTests。Mac 上另外使用 install-update.swift 的 UPDATE_REPLACEMENT_TESTS 编译开关运行事务测试，再在隔离安装中验证正常升级及回退；不要以静态字符串检查代替安装验收。

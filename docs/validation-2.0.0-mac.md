# Mac 2.0.0 发布验收

源码：`b9acad04f9042a1f47903db8fbe6c95066c51b8c`。安装包为 Apple Silicon ARM64，版本 2.0.0（2000099）。

- Developer ID 签名、Apple 公证、票据装订和 Gatekeeper 验证通过。
- 105 项 Mac 检查、19 项模型生命周期测试、四语言文案检查与下载通道检查通过，NuGet 未报告已知漏洞。
- 真实配音、最终成果回执、成品入库和字幕短样本通过；内嵌工作台可正常显示。
- 四语言引擎启动日志与原始输出保留通过；模型维护进度和摘要支持四语言。
- 日志目录只读或被同名文件占用时，模型检查仍可正常完成，界面恢复可操作状态。
- Beta 4 升级至 2.0.0、重启、显式回退至 1.9.9 及旧版完成提示兼容性通过；普通更新不会隐式降级。
- 隔离测试配置、项目和输出文件校验一致；历史版本和既有 Windows 附件保留。

实机系统为 macOS 27 beta。未覆盖所有机型、其他受支持系统版本、全部模型和长任务。模型效果需结合具体素材与设备确认。

Mac 使用独立源码压缩包；Windows 的既有安装包与源码对应原 2.0.0 发布提交。
